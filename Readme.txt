GUI Lesson project that we focused three main page that when the user comes the web site, which colors , which buttons, which orders, etc
have big impact on the performance and reliability on that web site. 